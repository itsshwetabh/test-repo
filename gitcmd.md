# git clone 'Link'
# change drive
# git switch -C anas
# git add .
# git commit -m 'Msg'
# git push origin (myBranch)

# Additional Info:-
1) git status
2) git branch
3) switch(checkout)
4) git stash --> Undo
5) git fetch
6) Added new command